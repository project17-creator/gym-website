<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IRON & PULSE | Elevate Your Fitness</title>
    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            --bg-color: #0f0f10;
            --card-bg: #1a1a1c;
            --text-main: #ffffff;
            --text-muted: #a0a0a0;
            --accent: #d4f126; /* Neon Lime */
            --accent-hover: #b8d11e;
            --transition: all 0.3s ease-in-out;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            overflow-x: hidden;
        }

        /* --- UTILITIES --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background-color: var(--accent);
            color: var(--bg-color);
            font-weight: 700;
            text-transform: uppercase;
            text-decoration: none;
            border-radius: 50px;
            transition: var(--transition);
            letter-spacing: 1px;
            cursor: pointer;
            border: none;
        }

        .btn:hover {
            background-color: var(--accent-hover);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(212, 241, 38, 0.2);
        }

        .section-title {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1rem;
            text-transform: uppercase;
            font-weight: 800;
        }

        .section-subtitle {
            text-align: center;
            color: var(--text-muted);
            margin-bottom: 4rem;
        }

        /* --- ANIMATION CLASSES --- */
        .hidden {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s ease-out;
        }

        .show {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- NAVIGATION --- */
        nav {
            position: fixed;
            width: 100%;
            top: 0;
            background: rgba(15, 15, 16, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 20px 0;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 900;
            color: var(--text-main);
            letter-spacing: -1px;
        }

        .logo span { color: var(--accent); }

        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            color: var(--text-main);
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
        }

        .nav-links a:hover { color: var(--accent); }

        .hamburger { display: none; cursor: pointer; }
        .bar { width: 25px; height: 3px; background-color: white; margin: 5px 0; transition: 0.3s; }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), 
                        url('https://images.unsplash.com/photo-1534438327276-14e5300c3a48?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80');
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding-top: 80px;
        }

        .hero-content h1 {
            font-size: 4rem;
            text-transform: uppercase;
            line-height: 1.1;
            margin-bottom: 20px;
            font-weight: 900;
        }

        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: #ddd;
        }

        /* --- CLASSES --- */
        .classes { padding: 100px 0; }
        
        .class-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .class-card {
            background: var(--card-bg);
            border-radius: 15px;
            overflow: hidden;
            position: relative;
            transition: var(--transition);
        }

        .class-card:hover { transform: scale(1.02); }

        .class-img {
            height: 200px;
            width: 100%;
            object-fit: cover;
        }

        .class-info { padding: 25px; }
        .class-info h3 { font-size: 1.5rem; margin-bottom: 10px; }
        .class-info p { color: var(--text-muted); margin-bottom: 15px; }

        /* --- PRICING --- */
        .pricing { padding: 100px 0; background-color: #151516; }

        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 40px;
        }

        .price-card {
            background: var(--card-bg);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            border: 1px solid rgba(255,255,255,0.05);
            transition: var(--transition);
            position: relative;
        }

        .price-card.popular {
            border: 1px solid var(--accent);
            box-shadow: 0 0 30px rgba(212, 241, 38, 0.1);
        }

        .price-tag {
            font-size: 3rem;
            font-weight: 800;
            margin: 20px 0;
            color: var(--accent);
        }

        .price-tag span { font-size: 1rem; color: var(--text-muted); font-weight: 400; }

        .features-list {
            list-style: none;
            margin-bottom: 30px;
            text-align: left;
        }

        .features-list li {
            margin-bottom: 10px;
            color: #ccc;
            display: flex;
            align-items: center;
        }

        .features-list li::before {
            content: '✓';
            color: var(--accent);
            margin-right: 10px;
            font-weight: bold;
        }

        /* --- CONTACT/FOOTER --- */
        footer {
            padding: 80px 0 40px;
            border-top: 1px solid rgba(255,255,255,0.1);
            text-align: center;
        }

        .footer-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 50px;
            margin-bottom: 50px;
        }

        .footer-col h4 { color: var(--accent); margin-bottom: 20px; text-transform: uppercase; }
        .footer-col p { color: var(--text-muted); margin-bottom: 10px; }

        /* --- MOBILE RESPONSIVENESS --- */
        @media (max-width: 768px) {
            .nav-links {
                position: fixed;
                right: -100%;
                top: 70px;
                height: calc(100vh - 70px);
                background: var(--bg-color);
                flex-direction: column;
                width: 100%;
                text-align: center;
                padding-top: 50px;
                transition: 0.3s;
            }

            .nav-links.active { right: 0; }
            .hamburger { display: block; }
            .hero-content h1 { font-size: 2.5rem; }
        }
    </style>
</head>
<body>

    <!-- NAVIGATION -->
    <nav>
        <div class="container nav-content">
            <a href="#" class="logo">IRON<span>&</span>PULSE</a>
            <div class="nav-links">
                <a href="#classes">Classes</a>
                <a href="#pricing">Membership</a>
                <a href="#contact">Contact</a>
                <a href="#pricing" class="btn" style="padding: 8px 20px; font-size: 0.9rem;">Join Now</a>
            </div>
            <div class="hamburger">
                <div class="bar"></div>
                <div class="bar"></div>
                <div class="bar"></div>
            </div>
        </div>
    </nav>

    <!-- HERO SECTION -->
    <section class="hero">
        <div class="container hero-content hidden">
            <h1>Forge Your<br> <span style="color: var(--accent);">Ultimate Self</span></h1>
            <p>No excuses. Just results. Join the most aesthetic training ground in the city.</p>
            <a href="#pricing" class="btn">Start 7-Day Free Trial</a>
        </div>
    </section>

    <!-- CLASSES SECTION -->
    <section id="classes" class="classes">
        <div class="container">
            <h2 class="section-title hidden">Our Training</h2>
            <p class="section-subtitle hidden">Designed for every fitness level</p>
            
            <div class="class-grid">
                <!-- Card 1 -->
                <div class="class-card hidden">
                    <img src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Weights" class="class-img">
                    <div class="class-info">
                        <h3>Strength & Hypertrophy</h3>
                        <p>Heavy lifting focused on building raw power and aesthetic muscle.</p>
                    </div>
                </div>
                <!-- Card 2 -->
                <div class="class-card hidden" style="transition-delay: 100ms;">
                    <img src="https://images.unsplash.com/photo-1518611012118-696072aa579a?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="HIIT" class="class-img">
                    <div class="class-info">
                        <h3>Neon HIIT</h3>
                        <p>High intensity interval training under neon lights. Burn 800+ calories.</p>
                    </div>
                </div>
                <!-- Card 3 -->
                <div class="class-card hidden" style="transition-delay: 200ms;">
                    <img src="https://images.unsplash.com/photo-1599058945522-28d584b6f0ff?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Yoga" class="class-img">
                    <div class="class-info">
                        <h3>Recovery Flow</h3>
                        <p>Mobility and yoga sessions to prevent injury and improve flexibility.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- PRICING SECTION -->
    <section id="pricing" class="pricing">
        <div class="container">
            <h2 class="section-title hidden">Membership</h2>
            <p class="section-subtitle hidden">Simple pricing. No hidden fees.</p>
            
            <div class="pricing-grid">
                <!-- Basic -->
                <div class="price-card hidden">
                    <h3>Starter</h3>
                    <div class="price-tag">$29<span>/mo</span></div>
                    <ul class="features-list">
                        <li>Access to Gym Floor</li>
                        <li>Locker Room Access</li>
                        <li>1 Guest Pass / Month</li>
                    </ul>
                    <a href="#" class="btn" style="background: transparent; border: 1px solid white; color: white;">Select</a>
                </div>

                <!-- Popular -->
                <div class="price-card popular hidden" style="transition-delay: 100ms;">
                    <div style="position:absolute; top:0; right:0; background:var(--accent); color:black; padding:5px 15px; border-bottom-left-radius:10px; font-weight:bold;">BEST VALUE</div>
                    <h3>Athlete</h3>
                    <div class="price-tag">$59<span>/mo</span></div>
                    <ul class="features-list">
                        <li>24/7 Gym Access</li>
                        <li>Unlimited Classes</li>
                        <li>Free Personal Training Session</li>
                        <li>Sauna Access</li>
                    </ul>
                    <a href="#" class="btn">Join Now</a>
                </div>

                <!-- Elite -->
                <div class="price-card hidden" style="transition-delay: 200ms;">
                    <h3>Elite</h3>
                    <div class="price-tag">$99<span>/mo</span></div>
                    <ul class="features-list">
                        <li>All Athlete Perks</li>
                        <li>Weekly Personal Training</li>
                        <li>Nutrition Plan</li>
                        <li>Private Locker</li>
                    </ul>
                    <a href="#" class="btn" style="background: transparent; border: 1px solid white; color: white;">Select</a>
                </div>
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer id="contact">
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <h4>Visit Us</h4>
                    <p>123 Fitness Blvd</p>
                    <p>Los Angeles, CA 90210</p>
                </div>
                <div class="footer-col">
                    <h4>Hours</h4>
                    <p>Mon-Fri: 5am - 11pm</p>
                    <p>Sat-Sun: 7am - 9pm</p>
                </div>
                <div class="footer-col">
                    <h4>Contact</h4>
                    <p>(555) 123-4567</p>
                    <p>hello@ironpulse.com</p>
                </div>
            </div>
            <p style="color: #666; font-size: 0.8rem;">&copy; 2023 Iron & Pulse Gym. All rights reserved.</p>
        </div>
    </footer>

    <!-- JAVASCRIPT -->
    <script>
        // 1. Mobile Navigation Toggle
        const hamburger = document.querySelector('.hamburger');
        const navLinks = document.querySelector('.nav-links');

        hamburger.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        // 2. Scroll Animation (Intersection Observer)
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('show');
                }
            });
        });

        const hiddenElements = document.querySelectorAll('.hidden');
        hiddenElements.forEach((el) => observer.observe(el));
    </script>
</body>
</html>